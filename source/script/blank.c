#include "parser.h"

#include "character.h"

int skipBlanks( Parser* parser , const char character ) {
    //int result ;
    //while( parser->character == ' ' || parser->character != '\n' || parser->character == '\r' || parser->character != '\t' )
    //    if( result = readCharacter( parser , in ) )
    return 0 ;
}